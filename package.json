{
  "name": "codex-responses-translator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local protocol translator: OpenAI Responses API to Chat Completions API for Codex CLI.",
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "test": "node --test tests/*.test.ts",
    "check": "node --check src/index.ts",
    "healthcheck": "bash scripts/healthcheck.sh"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
